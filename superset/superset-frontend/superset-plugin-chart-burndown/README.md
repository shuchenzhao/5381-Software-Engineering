# Superset Burndown Chart Plugin

This plugin provides a simple burndown chart for Superset. It's a small plugin.

Data expectations:
- x column: `ds` (date)
- y column: `remaining` (remaining work)

To run the plugin, run the script file run_burndown_storybook.sh